# Project Memory

- This project demonstrates OpenCode agents, commands, skills, tools and MCPs.
- Prefer small, inspectable examples for teaching.
- Validate changed HTML with `html-check`.
- Validate changed Java files with `java-check`.
- Use `docs/` for stable architecture and decisions.
- Use `tasks/` for persistent task tracking.

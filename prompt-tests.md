/goal
Create a new file named clock.html that shows an analog circular watch updated every second and drawn on a canvas, make it look like a swiss trains watch search on the internet to know how it is.

/goal
Create a new file named snake.html with a playable Snake game using only HTML, CSS, and JavaScript. Use canvas, keyboard arrows to control direction, score display, pause/restart buttons, and run the HTML checker.

/goal
Create a new file named slider.html with a slider puzle 9x9 that moves the clicked piece to the empty space, make the pieces with big numbers from 1 to 8 and making it solved when all numbers are properly sorted and the empty space is at bottom right corner. Don't use canvas, each piece must be a `<div>`.

/goal
Create a new todo website (todo.html, todo.css and todo.js) with a polished todo app using localStorage. Include add, edit, complete, delete, filters, clear completed, keyboard support, responsive layout, use safe-edit, and run the HTML checker.

/goal
Create a new weather-card website (weather.html, weather.css, weather.js) with a polished weather card UI. Search the web for the current weather in Barcelona, then build a static HTML page showing the city, temperature, condition, humidity, wind, and update time. Use only HTML, CSS, and JavaScript, cite the source visibly in the page, use safe-edit, and run the HTML checker.

/goal
Create a new travel-guide website (travel.html, travel.css, travel.js) with a mini travel guide for Girona. Search the web for 5 recommended places to visit, opening hours when available, short descriptions, and official links. Build a responsive card-based page with search/filter by category. Use only HTML, CSS, and JavaScript, cite sources, use safe-edit, and run the HTML checker.


# web-check MCP

Checks HTML, CSS, and JS files.

Tool:

- `check_web`: validates HTML tag balance, local asset presence, CSS syntax, and JS syntax.

Test:

```sh
npm --prefix .opencode/mcp/web-check test
```

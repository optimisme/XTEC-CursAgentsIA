# web_research MCP

Small web research MCP for local models that struggle with free-form `websearch` and `webfetch` loops.

Tools:

- `search`: search the web once and return compact JSON results. Optional parameters can limit to a site or request fresh results.
- `fetch_summary`: fetch one URL and return title, dates, description, and a short summary.

The tools do not read or write project files.

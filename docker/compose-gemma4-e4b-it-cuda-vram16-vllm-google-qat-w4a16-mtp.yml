# Stop:    docker compose -f compose-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp.yml down
# Restart: docker compose -f compose-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp.yml up -d
# Logs:    docker logs -f --tail 80 gemma4_e4b_it_cuda_vram16_vllm_google_qat_w4a16_mtp
# Note:    Gemma 4 E4B QAT compressed-tensors checkpoint for native vLLM serving.
#          This cuda-vram16 profile enables 32k context, text-only input, and Gemma 4 MTP.
#          Remote host port 8000 maps to the OpenAI-compatible vLLM API.
services:
  gemma-vllm:
    image: vllm/vllm-openai:v0.23.0
    pull_policy: missing
    container_name: gemma4_e4b_it_cuda_vram16_vllm_google_qat_w4a16_mtp
    environment:
      - HF_HOME=/root/.cache/huggingface
      - HUGGINGFACE_HUB_CACHE=/root/.cache/huggingface/hub
      - VLLM_CACHE_ROOT=/root/.cache/vllm
      - PYTORCH_ALLOC_CONF=expandable_segments:True
      - HF_HUB_DISABLE_XET=1
    volumes:
      - ./tokens.env:/run/secrets/tokens.env:ro
      - xtec-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp-hf-cache:/root/.cache/huggingface
      - xtec-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp-vllm-cache:/root/.cache/vllm
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/v1/models"]
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 30m
    ipc: host
    shm_size: 16g
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    entrypoint:
      - /bin/bash
      - -lc
    command:
      - |
        set -a
        . /run/secrets/tokens.env
        set +a
        if [ -n "$${HUGGINGFACE_ACCESS_TOKENS:-}" ]; then
          export HF_TOKEN="$${HUGGINGFACE_ACCESS_TOKENS}"
          export HUGGING_FACE_HUB_TOKEN="$${HUGGINGFACE_ACCESS_TOKENS}"
        fi
        python3 - <<'PY'
        import json
        from pathlib import Path

        snapshot_root = Path("/root/.cache/huggingface/hub/models--google--gemma-4-E4B-it-qat-w4a16-ct/snapshots")
        for config_path in snapshot_root.glob("*/config.json"):
            config = json.loads(config_path.read_text())
            vision_config = config.get("vision_config")
            if isinstance(vision_config, dict) and "num_soft_tokens" not in vision_config:
                num_soft_tokens = vision_config.get("mm_posemb_size")
                if num_soft_tokens is not None:
                    vision_config["num_soft_tokens"] = num_soft_tokens
                    config_path.write_text(json.dumps(config, indent=2) + "\n")
                    print(f"Patched {config_path}: vision_config.num_soft_tokens={num_soft_tokens}")
        PY
        exec vllm serve google/gemma-4-E4B-it-qat-w4a16-ct \
          --served-model-name active-model \
          --host 0.0.0.0 \
          --port 8000 \
          --dtype bfloat16 \
          --max-model-len 32768 \
          --gpu-memory-utilization 0.92 \
          --kv-cache-dtype fp8 \
          --max-num-seqs 1 \
          --max-num-batched-tokens 4096 \
          --enable-prefix-caching \
          --enable-chunked-prefill \
          --trust-remote-code \
          --limit-mm-per-prompt '{"image": 0, "audio": 0}' \
          --generation-config auto \
          --speculative-config '{"method":"mtp","model":"google/gemma-4-E4B-it-qat-q4_0-unquantized-assistant","num_speculative_tokens":1}' \
          --override-generation-config '{"temperature": 0, "top_p": 0.95, "top_k": 64}' \
          --reasoning-parser gemma4 \
          --enable-auto-tool-choice \
          --tool-call-parser gemma4 \
          --chat-template examples/tool_chat_template_gemma4.jinja \
          --default-chat-template-kwargs '{"enable_thinking": true}'

volumes:
  xtec-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp-hf-cache:
    name: xtec-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp-hf-cache
    external: true
  xtec-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp-vllm-cache:
    name: xtec-gemma4-e4b-it-cuda-vram16-vllm-google-qat-w4a16-mtp-vllm-cache
    external: true

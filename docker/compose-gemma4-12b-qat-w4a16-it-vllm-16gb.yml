# Stop:    docker compose -f compose-gemma4-12b-qat-w4a16-it-vllm-16gb.yml down
# Restart: docker compose -f compose-gemma4-12b-qat-w4a16-it-vllm-16gb.yml up -d
# Logs:    docker logs -f --tail 80 gemma4_12b_qat_w4a16_it_vllm_16gb
# Note:    Gemma 4 12B QAT compressed-tensors checkpoint for native vLLM serving.
#          Remote host port 8000 maps to the OpenAI-compatible vLLM API.
services:
  gemma-vllm:
    image: vllm/vllm-openai:cu129-nightly
    pull_policy: always
    container_name: gemma4_12b_qat_w4a16_it_vllm_16gb
    environment:
      - HF_HOME=/root/.cache/huggingface
      - HUGGINGFACE_HUB_CACHE=/root/.cache/huggingface/hub
      - VLLM_CACHE_ROOT=/root/.cache/vllm
      - PYTORCH_ALLOC_CONF=expandable_segments:True
      - HF_HUB_DISABLE_XET=1
    volumes:
      - ./tokens.env:/run/secrets/tokens.env:ro
      - xtec-hf-cache:/root/.cache/huggingface
      - xtec-vllm-cache:/root/.cache/vllm
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/v1/models"]
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 30m
    ipc: host
    shm_size: 16g
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    entrypoint:
      - /bin/bash
      - -lc
    command:
      - |
        set -a
        . /run/secrets/tokens.env
        set +a
        if [ -n "$${HUGGINGFACE:-}" ]; then
          export HF_TOKEN="$${HUGGINGFACE}"
          export HUGGING_FACE_HUB_TOKEN="$${HUGGINGFACE}"
        fi
        python3 - <<'PY'
        import json
        from pathlib import Path

        snapshot_root = Path("/root/.cache/huggingface/hub/models--google--gemma-4-12B-it-qat-w4a16-ct/snapshots")
        for config_path in snapshot_root.glob("*/config.json"):
            config = json.loads(config_path.read_text())
            vision_config = config.get("vision_config")
            if isinstance(vision_config, dict) and "num_soft_tokens" not in vision_config:
                num_soft_tokens = vision_config.get("mm_posemb_size")
                if num_soft_tokens is not None:
                    vision_config["num_soft_tokens"] = num_soft_tokens
                    config_path.write_text(json.dumps(config, indent=2) + "\n")
                    print(f"Patched {config_path}: vision_config.num_soft_tokens={num_soft_tokens}")
        PY
        exec vllm serve google/gemma-4-12B-it-qat-w4a16-ct \
          --served-model-name active-model \
          --host 0.0.0.0 \
          --port 8000 \
          --max-model-len 32768 \
          --gpu-memory-utilization 0.90 \
          --max-num-seqs 1 \
          --max-num-batched-tokens 4096 \
          --enable-prefix-caching \
          --enable-chunked-prefill \
          --trust-remote-code \
          --limit-mm-per-prompt '{"image": 0, "audio": 0}' \
          --generation-config auto \
          --override-generation-config '{"temperature": 1.0, "top_p": 0.95, "top_k": 64}' \
          --reasoning-parser gemma4 \
          --enable-auto-tool-choice \
          --tool-call-parser gemma4 \
          --chat-template examples/tool_chat_template_gemma4.jinja \
          --default-chat-template-kwargs '{"enable_thinking": true}'

volumes:
  xtec-hf-cache:
    name: xtec-hf-cache
    external: true
  xtec-vllm-cache:
    name: xtec-vllm-cache
    external: true

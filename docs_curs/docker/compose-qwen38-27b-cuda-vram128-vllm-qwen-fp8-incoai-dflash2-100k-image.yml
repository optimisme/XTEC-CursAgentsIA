# Stop:    docker compose -f compose-qwen38-27b-cuda-vram128-vllm-qwen-fp8-incoai-dflash2-100k-image.yml down
# Start:   docker compose -f compose-qwen38-27b-cuda-vram128-vllm-qwen-fp8-incoai-dflash2-100k-image.yml up -d
# Logs:    docker logs -f --tail 80 qwen38_27b_cuda_vram128_vllm_incoai_dflash2_100k_image
# Note:    Official Qwen FP8 target + IncoAI DFlash2 requires the vLLM PR 52816 code and V2 model runner.
services:
  qwen-vllm:
    image: vllm/vllm-openai:nightly
    pull_policy: missing
    container_name: qwen38_27b_cuda_vram128_vllm_incoai_dflash2_100k_image
    environment:
      - HF_HOME=/root/.cache/huggingface
      - HUGGINGFACE_HUB_CACHE=/root/.cache/huggingface/hub
      - VLLM_CACHE_ROOT=/root/.cache/vllm
      - PIP_CACHE_DIR=/root/.cache/pip
      - LD_LIBRARY_PATH=/usr/local/lib/python3.12/dist-packages/nvidia/cu13/lib:/usr/local/cuda/lib64
      - CMAKE_ARGS=-DCUDA_nvrtc_LIBRARY=/usr/local/lib/python3.12/dist-packages/nvidia/cu13/lib/libnvrtc.so.13
      - TORCH_CUDA_ARCH_LIST=12.1
      - PYTORCH_ALLOC_CONF=expandable_segments:True
      - HF_HUB_DISABLE_XET=1
      - HF_HUB_ENABLE_HF_TRANSFER=1
      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1
      - VLLM_USE_V2_MODEL_RUNNER=1
      - TORCH_MATMUL_PRECISION=high
    volumes:
      - ./tokens.env:/run/secrets/tokens.env:ro
      - xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-hf-cache:/root/.cache/huggingface
      - xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-vllm-cache:/root/.cache/vllm
      - xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-pip-cache:/root/.cache/pip
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/v1/models"]
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 60m
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    entrypoint:
      - /bin/bash
      - -lc
    command:
      - |
        set -e
        set -a
        . /run/secrets/tokens.env
        set +a
        if [ -n "$${HUGGINGFACE_ACCESS_TOKENS:-}" ]; then
          export HF_TOKEN="$${HUGGINGFACE_ACCESS_TOKENS}"
          export HUGGING_FACE_HUB_TOKEN="$${HUGGINGFACE_ACCESS_TOKENS}"
        fi
        if ! python3 -c 'import vllm.model_executor.models.qwen3_dflash2' >/dev/null 2>&1; then
          pr_base="https://raw.githubusercontent.com/vllm-project/vllm/19c9351904df4c63042671bc67a866ca48dc7d6f"
          vllm_root="$$(python3 -c 'import os,vllm; print(os.path.dirname(vllm.__file__))')"
          for rel in \
            vllm/config/vllm.py \
            vllm/model_executor/models/qwen3_dflash.py \
            vllm/model_executor/models/qwen3_dflash2.py \
            vllm/model_executor/models/registry.py \
            vllm/v1/worker/gpu/spec_decode/__init__.py \
            vllm/v1/worker/gpu/spec_decode/dflash2/__init__.py \
            vllm/v1/worker/gpu/spec_decode/dflash2/speculator.py; do
            dest="$${vllm_root}/$${rel#vllm/}"
            mkdir -p "$$(dirname "$$dest")"
            curl -fsSL "$$pr_base/$$rel" -o "$$dest"
          done
        fi
        exec vllm serve Qwen/Qwen3.8-27B-FP8 \
          --served-model-name active-model \
          --host 0.0.0.0 \
          --port 8000 \
          --tensor-parallel-size 1 \
          --trust-remote-code \
          --dtype auto \
          --kv-cache-dtype auto \
          --gpu-memory-utilization 0.85 \
          --max-model-len 100000 \
          --max-num-seqs 32 \
          --max-num-batched-tokens 8192 \
          --enable-chunked-prefill \
          --enable-prefix-caching \
          --limit-mm-per-prompt '{"image":5,"video":0}' \
          --generation-config auto \
          --override-generation-config '{"temperature":1.0,"top_p":0.95,"top_k":20,"min_p":0.0,"presence_penalty":0.0,"repetition_penalty":1.0}' \
          --speculative-config '{"method":"dflash","model":"incoai/Qwen3.8-27B-DFlash2","num_speculative_tokens":7}' \
          --reasoning-parser qwen3 \
          --tool-call-parser qwen3_coder \
          --enable-auto-tool-choice

volumes:
  xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-hf-cache:
    name: xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-hf-cache
    external: true
  xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-vllm-cache:
    name: xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-vllm-cache
    external: true
  xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-pip-cache:
    name: xtec-qwen38-27b-cuda-vram128-vllm-qwen-fp8-dflash2-128k-image-pip-cache
    external: true
